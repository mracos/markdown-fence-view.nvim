# markdown-fence-view extraction manifest (see scripts/extract-nvim.sh).
DESCRIPTION="Spec-driven render-markdown.nvim handler that executes fenced code blocks and renders their output inline"

# ADRs to ship with the mirror, as globs under docs/adrs/ (copied verbatim).
ADRS=(
  "editors/0002-editors-nvim-markdown-fence-view.md"
  "editors/0003-editors-nvim-query-view-writeback.md"
  "editors/0005-editors-nvim-persist-inline-results.md"
)

# COMPANION_CLI: deferred. When set, the extract step will render a runtime PATH
# check + a README requirement line (see ADR editors/0008, open question).
